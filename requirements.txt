# 核心依赖
PyYAML>=6.0
requests>=2.28
certifi>=2023.0        # python.org 版 Python 默认无根证书，靠它做 SSL 校验

# 扫码自动抓登录态（update_session.py）用；若只手填 token/cookie 可不装
# 装完还需： python3 -m playwright install chromium
playwright>=1.40

# 仅 rss 备用采集源用到
feedparser>=6.0
